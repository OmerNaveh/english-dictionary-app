// GET /:word/:partOfSpeech - will return a word + definition + part of speech (noun, verb, adjectives, etc...)

const getByWordAndSpeechType = (req, res, next) => {};

module.exports = { getByWordAndSpeechType };
