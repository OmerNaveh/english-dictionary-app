// GET /:word - if word has more than one parts of speech will return all words part of speech, else, will return a word + definition + part of speech.

const getByWord = (req, res, next) => {};

module.exports = { getByWord };
